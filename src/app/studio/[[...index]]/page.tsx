'use client'

import SanityStudioWrapper from '@/components/SanityStudioWrapper'
import config from '../../../../sanity.config'

export default function StudioPage() {
  return <SanityStudioWrapper config={config} />
} 